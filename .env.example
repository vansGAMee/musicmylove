# Public URL of the SEPARATELY deployed gateway, baked into the static build.
NEXT_PUBLIC_GATEWAY_URL=
# Gateway settings (not Vercel Functions)
FRONTEND_ORIGIN=http://localhost:3000
PORT=8787
# Optional batch search provider returning verified context evidence.
CONTEXT_BATCH_URL=
CONTEXT_TOKEN=
# Offline build inputs; real audio vectors are optional, never synthesized.
AUDIO_VECTORS=
WEB_CONTEXT=
